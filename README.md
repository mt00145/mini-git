# ugit

A minimal Git implementation written in Python.

This project implements core version control concepts including:
- content-addressed object storage
- commits and trees
- branches and tags
- merge and diff
- basic remote push/pull functionality

Built as a learning project to understand how Git works internally.